#ifndef METRIC_H
#define METRIC_H

#include <stdlib.h>
#include <stdio.h>

typedef struct s_position {
	float x;
	float y;
}sPosition;

typedef struct s_dimension{
	int height;
	int width;
}sDimension;

#endif